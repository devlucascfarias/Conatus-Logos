# Dependências específicas do notebook de treino (M5, seção 12) — instalar só no Colab.
# Versões fixadas aqui (seção 12.2: nenhuma célula sem pinning) — baseline testado localmente
# (CPU) nesta geração do projeto via tests/unit/test_transformers_runner.py.
#
# IMPORTANTE: `torch` (e `torchvision`, se presente) NÃO são fixados aqui de propósito — o
# Colab já vem com um par torch+torchvision pré-instalado, compatível entre si e com a build
# CUDA/driver da GPU alocada (L4). Fixar uma versão de `torch` aqui força uma reinstalação
# que desalinha esse par (torchvision compilado contra uma versão de torch diferente da que
# fica instalada depois), causando erros como "operator torchvision::nms does not exist" ao
# carregar qualquer modelo via transformers. Deixe o torch do Colab como está; só os pacotes
# abaixo (que não trazem seu próprio requisito de build CUDA) são de fato pinados.

transformers==5.10.2
peft==0.19.1
bitsandbytes==0.49.2
trl==1.5.1
accelerate==1.10.1
datasets>=2.19

# D-torchao: peft==0.19.1 checks torchao's version at import time (`is_torchao_available()`)
# whenever the package is present, and raises ImportError if it's below 0.16.0 — even though
# this project never uses torchao (quantization here is bitsandbytes-only). Colab's base image
# ships an older torchao that fails this check, breaking `from peft import PeftModel` at eval
# time (confirmed: "Found version 0.10.0, but only versions above 0.16.0 are supported").
# Pinning >=0.16.0 here keeps a fresh `pip install -r requirements-train.txt` clean.
torchao>=0.16.0
