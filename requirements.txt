# Dependências do harness de referência, checker, dataset pipeline e testes (M0-M4, M6).
# Dependências de treino (M5, só necessárias dentro do notebook/Colab) ficam em
# requirements-train.txt para não obrigar quem só roda harness/checker a instalar
# transformers/bitsandbytes/etc.

jsonschema>=4.20
PyYAML>=6.0
requests>=2.31
pytest>=8.0

# Backend "html" do checker (docs/plan_frontend_specialization_wave3.md seção 3) — renderização
# real em Chromium headless para verificar erro de console/exceção JS. Depois de instalar,
# rode uma vez: python -m playwright install chromium
playwright>=1.49
