{
  "metadata": {
    "id": "gen-react-usememo-usecallback-perf",
    "domain": "frontend",
    "language": "typescript",
    "difficulty": "hard",
    "tools_used": [
      "write_file",
      "checker"
    ],
    "num_steps": 2,
    "task_type": "multi_tool_call",
    "source": "curated_manual_real_execution",
    "license": "synthetic-no-license-needed",
    "validation_status": "validated",
    "execution_performed": true,
    "split": "train",
    "checker_used": "checker-typescript-1.0",
    "expected_result": {
      "passed": true
    },
    "execution_classification": "interpreted"
  },
  "trajectory": {
    "system_prompt": "Você é Logos-3, um agente de engenharia de software da família de modelos Conatus. Raciocine em <think>, use <tool_call name=\"...\"> quando precisar de uma ferramenta, e responda ao usuário só dentro de <final>.",
    "user_request": "Crie uma lista de itens com um total calculado (cálculo custoso) e um botão de contador não relacionado na mesma tela. Garanta que o total não é recalculado, e as linhas da lista não re-renderizam, quando só o contador não relacionado muda.",
    "raw_text": "<think>Dois problemas de performance distintos, duas ferramentas distintas. (1) expensiveTotal(items) sem useMemo rodaria de novo a CADA render do componente, inclusive quando só unrelatedCounter muda (que não afeta o total nenhum pouquinho) — useMemo(() => expensiveTotal(items), [items]) só recalcula quando `items` muda de identidade. (2) ItemRow é memo() (evita re-render se as props não mudarem), mas isso só funciona se as props forem REALMENTE as mesmas — uma função inline `onRemove={(id) => ...}` criada dentro do render do pai seria uma função NOVA a cada render, quebrando a memoização por completo. useCallback com dependências vazias mantém a mesma identidade de função entre renders, então ItemRow.memo funciona de verdade.</think><tool_call name=\"write_file\">{\"path\": \"src/App.tsx\", \"content\": \"import { memo, useCallback, useMemo, useState } from \\\"react\\\";\\n\\ntype Item = { id: number; value: number };\\n\\nfunction expensiveTotal(items: Item[]): number {\\n  // Simula um cálculo custoso — na prática seria algo como agregação sobre milhares de\\n  // itens; aqui o ponto é NÃO refazer isso a cada render se `items` não mudou.\\n  let total = 0;\\n  for (let i = 0; i < items.length; i++) total += items[i].value;\\n  return total;\\n}\\n\\nconst ItemRow = memo(function ItemRow({ item, onRemove }: { item: Item; onRemove: (id: number) => void }) {\\n  return (\\n    <li>\\n      {item.value}\\n      <button onClick={() => onRemove(item.id)}>Remover</button>\\n    </li>\\n  );\\n});\\n\\nexport default function App() {\\n  const [items, setItems] = useState<Item[]>([\\n    { id: 1, value: 10 },\\n    { id: 2, value: 20 },\\n  ]);\\n  const [unrelatedCounter, setUnrelatedCounter] = useState(0);\\n\\n  // Só recalcula quando `items` muda de verdade — não a cada render causado por\\n  // unrelatedCounter, que não afeta o total.\\n  const total = useMemo(() => expensiveTotal(items), [items]);\\n\\n  // Identidade estável entre renders (enquanto `items` não mudar) — sem isso, ItemRow\\n  // (memoizado) re-renderizaria de qualquer forma, porque receberia uma função `onRemove`\\n  // NOVA a cada render do pai, mesmo com React.memo.\\n  const handleRemove = useCallback((id: number) => {\\n    setItems((prev) => prev.filter((i) => i.id !== id));\\n  }, []);\\n\\n  return (\\n    <div>\\n      <p>Total: {total}</p>\\n      <button onClick={() => setUnrelatedCounter((c) => c + 1)}>Contador não relacionado: {unrelatedCounter}</button>\\n      <ul>\\n        {items.map((item) => (\\n          <ItemRow key={item.id} item={item} onRemove={handleRemove} />\\n        ))}\\n      </ul>\\n    </div>\\n  );\\n}\\n\"}</tool_call><tool_result name=\"write_file\" status=\"ok\">{\"path\": \"src/App.tsx\", \"bytes_written\": 1747, \"mode\": \"overwrite\"}</tool_result><think>Vou buildar e renderizar de verdade pra confirmar que compila e monta sem erro de console.</think><tool_call name=\"checker\">{\"language\": \"typescript\", \"operation\": \"run\", \"files\": [{\"path\": \"src/App.tsx\", \"content\": \"import { memo, useCallback, useMemo, useState } from \\\"react\\\";\\n\\ntype Item = { id: number; value: number };\\n\\nfunction expensiveTotal(items: Item[]): number {\\n  // Simula um cálculo custoso — na prática seria algo como agregação sobre milhares de\\n  // itens; aqui o ponto é NÃO refazer isso a cada render se `items` não mudou.\\n  let total = 0;\\n  for (let i = 0; i < items.length; i++) total += items[i].value;\\n  return total;\\n}\\n\\nconst ItemRow = memo(function ItemRow({ item, onRemove }: { item: Item; onRemove: (id: number) => void }) {\\n  return (\\n    <li>\\n      {item.value}\\n      <button onClick={() => onRemove(item.id)}>Remover</button>\\n    </li>\\n  );\\n});\\n\\nexport default function App() {\\n  const [items, setItems] = useState<Item[]>([\\n    { id: 1, value: 10 },\\n    { id: 2, value: 20 },\\n  ]);\\n  const [unrelatedCounter, setUnrelatedCounter] = useState(0);\\n\\n  // Só recalcula quando `items` muda de verdade — não a cada render causado por\\n  // unrelatedCounter, que não afeta o total.\\n  const total = useMemo(() => expensiveTotal(items), [items]);\\n\\n  // Identidade estável entre renders (enquanto `items` não mudar) — sem isso, ItemRow\\n  // (memoizado) re-renderizaria de qualquer forma, porque receberia uma função `onRemove`\\n  // NOVA a cada render do pai, mesmo com React.memo.\\n  const handleRemove = useCallback((id: number) => {\\n    setItems((prev) => prev.filter((i) => i.id !== id));\\n  }, []);\\n\\n  return (\\n    <div>\\n      <p>Total: {total}</p>\\n      <button onClick={() => setUnrelatedCounter((c) => c + 1)}>Contador não relacionado: {unrelatedCounter}</button>\\n      <ul>\\n        {items.map((item) => (\\n          <ItemRow key={item.id} item={item} onRemove={handleRemove} />\\n        ))}\\n      </ul>\\n    </div>\\n  );\\n}\\n\"}], \"entrypoint\": \"index.html\", \"timeout_ms\": 60000}</tool_call><tool_result name=\"checker\" status=\"ok\">{\"passed\": true, \"errors\": [], \"stdout\": \"\\u001b[36mvite v5.4.21 \\u001b[32mbuilding for production...\\u001b[36m\\u001b[39m\\ntransforming...\\n\\u001b[32m✓\\u001b[39m 31 modules transformed.\\nrendering chunks...\\ncomputing gzip size...\\n\\u001b[2mdist/\\u001b[22m\\u001b[32mindex.html                \\u001b[39m\\u001b[1m\\u001b[2m  0.34 kB\\u001b[22m\\u001b[1m\\u001b[22m\\u001b[2m │ gzip:  0.25 kB\\u001b[22m\\n\\u001b[2mdist/\\u001b[22m\\u001b[36massets/index-m4tXf1uH.js  \\u001b[39m\\u001b[1m\\u001b[2m217.03 kB\\u001b[22m\\u001b[1m\\u001b[22m\\u001b[2m │ gzip: 54.75 kB\\u001b[22m\\n\\u001b[32m✓ built in 658ms\\u001b[39m\\n\", \"stderr\": \"\", \"metadata\": {\"language\": \"typescript\", \"duration_ms\": 3670, \"console_warnings\": []}}</tool_result><final>Criei a lista com dois cuidados de performance: useMemo(() => expensiveTotal(items), [items]) evita recalcular o total quando só o contador não relacionado muda, e useCallback mantém a identidade estável de handleRemove entre renders — sem isso, ItemRow (memo()) re-renderizaria mesmo memoizado, porque receberia uma função nova a cada render do pai. Confirmei buildando e renderizando de verdade — sem erro de console.</final>"
  }
}
